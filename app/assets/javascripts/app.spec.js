// describe('ThingsListController', function() {

// 	beforeEach(module('house'));

// 	it('should create a "things" model with 3 things', inject(function($controller) {
// 		var scope = {};
// 		var ctrl = $controller('hingsListController', {$scope: scope});

// 		expect(scope.things.length).toBe(3);


// 	}));

// });